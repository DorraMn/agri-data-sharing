# ============================================
# SERVICE - Données Partagées
# ============================================

apiVersion: v1
kind: Service
metadata:
  name: donnees-partagees
  namespace: agri-system
  labels:
    app: donnees-partagees
spec:
  type: ClusterIP
  ports:
  - port: 8083
    targetPort: 8082
    protocol: TCP
    name: http
  selector:
    app: donnees-partagees
