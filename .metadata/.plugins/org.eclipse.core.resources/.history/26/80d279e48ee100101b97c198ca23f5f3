package com.example.demo.service;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.amqp.rabbit.annotation.RabbitListener;
import org.springframework.stereotype.Service;

@Service
public class MessageConsumer {

    private static final Logger logger = LoggerFactory.getLogger(MessageConsumer.class);

    /**
     * Consommation des messages venant de RabbitMQ
     */
    @RabbitListener(queues = "${rabbitmq.queue}")
    public void receiveMessage(String message) {
        try {
            logger.info("📩 Message reçu depuis RabbitMQ → {}", message);

            // 👉 Ici tu peux ajouter ton traitement métier
            // Exemple : parsing JSON, sauvegarde DB, etc.

        } catch (Exception e) {
            logger.error("❌ Erreur lors de la consommation du message : {}", message, e);
        }
    }
}
